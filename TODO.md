# TODO


## **提示词：待完成**
好像只要有一个标点符号不一样，fingerprint就不一样，这种去重方式太落后了，建议直接删，不需要指纹，毕竟项目注册表里是没有这个的，id就通过别的方法生成，id生成也需要大改，id前缀应该指示记忆类型（比如projRegisterMemId-asdfad1fa2sga1s和projMemId-asdfahggka2sga1s和factMemId-sdfagasdg12dsf以及eventMemId-adfagagjhjad12fsga1分别就是项目注册表记忆，项目子问题记录，事实记忆和事件记忆）

你上面讲的这些：'save 去重'，'fingerprint'，'update 判断这次是不是其实没变'其实都没用


search是怎么在拼接返回字段的时候参考，不能根据实际拿到的字段返回吗，非得看一下SEARCH_RESULT_FIELDS 裁剪字段吗

## **长期记忆rag系统建立**
根据 project-beta 库的框架，搞一个rag系统，让模型通过 mcp 接口进行记忆的存入和读取


1. memory-type 记忆类型、字段设计
   - project-record 记忆类型分表（大改
      起因：一个项目记录随着时间推移可能有很多次 debug 记录要存(发展到后期可能会很庞大)，一个项目不可能一条记忆，修改也麻烦，比起通过 update 重写，还是 append 一条更不易出错，虽然可以通过新增一个 append() 接口来实现，但是更好的方案是分表
      解决方案：逻辑分表，给每个 project 记忆一个项目id
      新增 save_project 接口

      编码问题：尽量不走全量重建编码，可以先利用原 project-record 表把2个新表分好（通过增量编码分别都搞好），然后再把 project-record 表删除，这样走的一条一条都是增量链路，避免了全量重建

      后续可以优化id，让id的开头指示该条记忆的类型，后面接一串随机数即可

      1) 分表1: child-project-detail
         - 设计一个 origin-main-project 字段（命名待定），对应 main-project 表的 project 摘要条目，且限定该字段的值必须是 main-project 表中的已有 project，否则拒绝存入
            - 
      2) 分表2: main-project（命名待定）
         - 存的就是当前项目下的各个 child 问题的摘要，摘要在立项的时候建立，如果必要的话，后期也可以通过 update 微调
   - field_record 字段：单独做数量控制。
      比如不是每个字段都值得进向量库作为检索文本。
      比如很短、噪声大、重复率高的字段，可以不建向量，或者单独降权。
      时间可以考虑做进去



2. mcp 接口设计
   1) merge 实现（暂定，目前实现方案太依赖模型能力）
      - 工具描述：可以整合几条记忆为一条新的
      - 参数设计：传入需要被整合的ids，以及整合的新记忆的字段（字段与save的参数一致
      - 整合逻辑：
         - 简单粗暴，直接删除ids对应的条目，然后mcp底层调用save直接把整合好的新记忆存了即可
   2) save 优化
      - 在 server 端异步完成（暂时没必要，增量重建并不是很消耗时间）
        - 不用让用户等建库完成了才能进行下一个对话，如果当正在入库的时候会影响到下一次调用，可以考虑把 save （或者后期的 merge 脚本）单独写成一个脚本，或者是另外写成一个记忆编辑的 mcp（把现有的一个rag-mcp 分成检索记忆mcp 和记忆编辑 mcp）
        - 分析以上各种方案的可行性
   3) search 优化
      - 
        - 参考： search 接口简单来说，它会在底层把所有的 SQLite 业务表通过一个统一的视图（public_memory_view）强行拼接成一个拥有 20 个固定字段的大宽表，然后统一根据 settings.yaml 中硬编码的 search_fields（如 short_summary, project_id 等）进行裁剪并返回给大模型。

      - topk的虚假满足情况（待解决）：可能topk=10然后命中8个字段记忆，那最终返回的就不满10条，想想怎么满足
        - 以下是老式记忆结构的解决方案：
          1. 当该接口被调用（即收到用户的 quary）时，本次 search 将始维护一个"待查询主记忆列表"（即先把当前的主记忆 json 读进来），本次查询将用"待查询主记忆列表"进行主数据溯源
          2. 将查询到并溯源到的主数据条目加入"待返回记忆列表"，然后将该主数据条目以及相关的 field_record 记忆从所维护的"待查询主记忆列表"删除（目的是为了本次 search 调用中的第二次查询能排除掉已经提取过的信息）
          3. 检测一下"待返回记忆列表"是否满足 top_k，如果不满足并且"待查询主记忆列表"不为空，再次查询并将查询到的主记忆条目加入"待查询主记忆列表"（同样需要删除本次查到的相关信息），这样循环查询
          4. 循环终止条件：
              1) "待查询主记忆列表"已经不存在可查询条目
              2) top_k被满足
              3) 查出来的主数据条目的置信度低于设定的阈值
          - 提一嘴，当命中 field_record 类型的记忆，需要先回源到主记忆源条目，然后把主记忆源条目加入待返回记忆列表， field_record 始终不作为返回内容
   4) update 优化（上方字段记忆正要解决这个问题）
      - 询问ai有没有更好的关于"降低ai注意力涣散问题"的建议，比如让它只关注和修改与事实相悖的部分
      - 更新内容：只更新与当前事实相悖的部分，其他内容保留原始文本
      - 新增内容：如果没有冲突，则直接新增内容，其他内容保留原始文本

3. 找机会开拓多模态嵌入需求：探寻可能性

4. 数据量上去后再做，属于结构优化
   - 把主记忆和 field_record 分成两个 FAISS 索引。
      现在它们混在一个索引里，field_record 数量一多，就会把主记忆淹没。
      更好的办法是：
      一个索引只放主记忆
      一个索引放 field_record
      查询时双路召回，再合并打分
   - 暂未定：把 row_to_id 从 JSON 挪到 SQLite 或更紧凑的二进制文件。

5. 询问 ai 关于服务化的实现，比如接入本地客户端和开放公网访问
 