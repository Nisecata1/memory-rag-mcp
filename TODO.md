#########################################################

1. **长期记忆rag系统建立**：根据 project-beta 库的框架，搞一个rag系统，让模型通过mcp进行存入和读取
   1. 考虑服务化，比如接入本地客户端和开放公网访问

   2. memory-type 记忆类型设计
      1. field_record 类型的记忆仅用于提升召回，search 返回的对象是回源之后的真正记录
         优化：看看回源之后将要 return 的主数据记录条数满不满足topk，不满足怎发么优化，比如接着上次的相似度分数，继续检索更低分的记录并回源，直到达到topk再return
         就是思考如何避免碎片太多导致每次只命中了一个主数据记录
      2. fact 类型

   3. 接口设计
      1. merge
         工具描述：可以整合几条记忆为一条新的
         参数设计：传入需要被整合的ids，以及整合的新记忆的字段（字段与save的参数一致
         整合逻辑：
         - 简单粗暴，直接删除ids对应的条目，然后mcp底层调用save直接把整合好的新记忆存了即可

      2. save优化
         先调用search检索并分析库内没有重复内容时才能调用，如果有，考虑先记录下来，等我有空主动 review 落库文件，然后叫ai去 merge
         所以目前可以暂时把 search 定义为几乎所有 记忆变更目的 的第一步调用

      3. search 也可以分字段查找（待定，暂时没有这个需求）

   4. save 在 mcp-server 端异步完成（暂时没必要，增量重建并不是很消耗时间）
      不用让用户等建库完成了才能进行下一个对话，如果当正在入库的时候会影响到下一次调用，可以考虑把 save （或者后期的 merge 脚本）单独写成一个脚本，或者是另外写成一个记忆编辑的mcp（把现有 mcp 分成检索记忆mcp 和记忆编辑mcp）。请分析以上各种方案的可行性

   5. 找机会开拓多模态嵌入需求

一个重要排查结论
跑烟雾测试时，测出来还是 31 秒，后来排查发现那不是系统真慢，而是测试脚本把模型路径写成了小写盘符 c:\\...，而缓存里的 meta.model 是大写 C:\\...。当前缓存校验是按字符串直接比较的，所以它误判缓存失效，退回了全量重建。
也就是说：
正式服务当前配置下，路径大小写是一致的，所以能命中缓存
但这套实现现在对 Windows 路径大小写有点脆弱
这个风险点在 server.py (line 816) 这段缓存校验里。
它不影响你现在正式使用，但它是一个真实的后续优化点：模型路径比较最好做标准化，不要直接比原始字符串。
